services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: runner
    ports:
      - "3000:3000"
    env_file:
      - ../.env.local
    volumes:
      - ../public/brand:/app/public/brand:ro
    restart: unless-stopped
