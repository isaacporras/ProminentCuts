services:
  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: deps
    command: npm run dev
    ports:
      - "3000:3000"
    env_file:
      - ../.env.local
    volumes:
      - ..:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
